name: network_traffic_inspector_example
description: Example app for the network_traffic_inspector package.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.6.2
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  network_traffic_inspector:
    path: ../
  dio: ^5.8.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
